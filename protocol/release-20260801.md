# V4 release protocol - 1 August 2026

The machine-readable protocol is the `protocol.json` written before numerical
work by `research/v4_publication_campaign.py`. The release target is
`results/release/`.

This rerun retains the July fixed factorial designs, seeds, repetition counts,
stopping tolerances, statistical unit, estimands, bootstrap procedure, and
success gates. Before the rerun began, it additionally:

- serialized the previously separate UCI application-panel design;
- required and recorded five single-thread environment controls;
- preserved all binary64-distinct robust-deviation thresholds; and
- aligned the lower-bound refresh point in the two adaptive comparators.

The July evidence is not canonical and remains recoverable from Git history.
All August numerical artifacts are regenerated from `results/release/` after
the complete campaign and exact-integration audit finish.

Release requires all repository tests, serialized gates, evidence hashes,
generated-text comparisons, anonymous-package scans, PDF builds, and visual
checks to pass.
